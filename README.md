overleaf → git clone (terminal, pas Python)
job fetch → requests + JSearch API (RapidAPI, gratuit limité)
AI tailoring → anthropic Python SDK
LaTeX compile → subprocess → pdflatex
browser automation → playwright (pip install playwright)
tracking → sqlite3 (built-in Python)
scheduling → schedule ou cron job (tous les 2 jours)

